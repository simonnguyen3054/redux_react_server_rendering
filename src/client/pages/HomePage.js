import React from "react";

const Home = () => {
  return (
    <div>
      <div>Home Component React</div>
      <button onClick={() => console.log("I'm Clicked")}>Click Me</button>
    </div>
  );
};

export default {
  Home
}
